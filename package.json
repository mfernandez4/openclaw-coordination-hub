{
  "name": "openclaw-coordination-hub",
  "version": "0.1.0",
  "description": "Real-time coordination layer for OpenClaw sub-agent orchestration",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "test:coverage": "npx vitest run --coverage"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "ioredis-mock": "^8.13.1",
    "jest": "^30.3.0",
    "vitest": "^4.1.2"
  }
}
